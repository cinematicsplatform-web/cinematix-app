# Public Assets Folder

Put your static assets here to serve them from the root URL `/`.

1. **`share-image.png`**: Upload your website share preview image directly into this folder with this exact name (`share-image.png`).
2. **`ic.logo.png`**: Upload or copy your website icon here with this exact name (`ic.logo.png`).
